var prompt = require('prompt-sync')();

const a = Number(prompt("Enter a:"));
const b = Number(prompt("Enter b:"));

console.log(`a + b = ${a + b}`);
console.log(`a - b = ${a - b}`);
console.log(`a * b = ${a * b}`);
console.log(`a / b = ${a / b}`);
